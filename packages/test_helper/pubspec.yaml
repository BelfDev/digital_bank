name: test_helper
description: A tailor-made abstraction for testing the app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter
  # Internal packages
  localization: any
  ds_components: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Linting
  flutter_lints: ^2.0.1

flutter:
