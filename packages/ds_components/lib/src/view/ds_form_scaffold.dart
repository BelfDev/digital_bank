import 'package:flutter/material.dart';

class DSFormScaffold extends StatelessWidget {
  const DSFormScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
